package `in`.vikram.simulationtask.const

object Const {

    const val DATABASE_VERSION = 1
    const val DB_NAME = "photo.db"

    object Error {
        const val NO_CACHED_DATA = "no cached data"
        const val UNABLE_TO_RESOLVE_HOST = "unable to resolve host"
        const val NO_INTERNET = "no internet"
        const val TRY_AGAIN = "pls try again"
    }
}